fn main() -> Result<(), Box<dyn std::error::Error>> {
    // External headless crate not present; keep example compiling.
    println!("Headless renderer example is disabled in this build.");
    Ok(())
}
